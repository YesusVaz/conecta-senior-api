# ============================================
# CONECTA SENIOR API - Variáveis de Ambiente
# ============================================
#
# ATENÇÃO: Este é um arquivo de exemplo (.env.example)
# Para usar em produção:
# 1. Copie este arquivo para .env
# 2. Substitua os valores pelos reais de produção
# 3. Nunca commite o arquivo .env no git
#

# === BANCO DE DADOS ===
DB_PASSWORD=sua_senha_postgres_super_segura

# === JWT SECURITY (OBRIGATÓRIO EM PRODUÇÃO) ===
# IMPORTANTE: Use uma chave de pelo menos 32 caracteres
JWT_SECRET=sua_chave_jwt_muito_segura_com_pelo_menos_32_caracteres_aqui
JWT_EXPIRATION=86400000

# === CORS CONFIGURATION ===
# Especifique os domínios permitidos para acessar a API
# Múltiplos domínios devem ser separados por vírgula
CORS_ALLOWED_ORIGINS=https://app.conectasenior.com.br,https://admin.conectasenior.com.br

# === LOGGING ===
LOGGING_LEVEL_ROOT=WARN

# === SERVER ===
PORT=8080
